/*const aboutProduct: HTMLElement | null = document.querySelector(
    ".product-list"
  );
  aboutProduct?.addEventListener("click", (event) => {
    const target = event.target as HTMLElement;
    const itemID = target.querySelector(".product-card")?.id;
    //console.log(itemID);
    if (target) {
      console.log(target.closest(".about-button"));
      console.log(itemID);
    }
  });*/